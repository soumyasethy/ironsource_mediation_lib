name: ironsource_mediation
description: A new flutter plugin project.
version: 0.0.1
author:
homepage:

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: '>=1.20.0'

dependencies:
  flutter:
    sdk: flutter
  logger: 1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: 5.0.5
  build_runner: 2.0.1

flutter:
  plugin:
    platforms:
      android:
        package: com.ironSource.ironsource_mediation
        pluginClass: IronSourceMediationPlugin
      ios:
        pluginClass: IronSourceMediationPlugin